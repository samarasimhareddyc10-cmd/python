heads=int(input("Enter number of heads:"))
legs=int(input("Enter number of legs:"))
flags=False

for cow in range(heads + 1):
    hen=heads-cow
    if (cow*4+hen*2==legs):
        print(cow)
        print(hen)
        flags=True
if flags==False:
    print("no solution")
